# `openbook.py`文档

这篇文章提供`openbook.py`的功能以及实现细节等。

## 功能

开局库是每个象棋引擎的必备工具，
因此我想要通过皮卡鱼蒸馏出一个开局库，
使用`SQLite3`存储开局库文件，
对于常见走法分支有更深的记录，
对于其它分支也有一定的掌握，
开局库训练自动挂起运行，
要求实现断点重复、多线程训练等以提升训练效率。

作为开局库训练代码，
`openbook.py`实现各种训练开局库的功能。

## 实现细节

对外暴露三个函数
